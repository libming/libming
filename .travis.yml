before_install:
  - sudo apt-get update && sudo apt-cache search php | grep dev
  - sudo apt-get update && sudo apt-get install php5-dev
  - ./autogen.sh

language: c

script:
  - sudo ln -s `which phpize5` /usr/local/bin/phpize
  - ./configure --enable-php
  - make
  - make check
