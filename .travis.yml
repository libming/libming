dist: xenial

before_install:
  - sudo apt-get update
  - apt-get -fmy install php
  - apt-get -fmy install php-all-dev
  - apt-get -fmy install php-cgi
  - apt-get -fmy install php-cli
  - apt-get -fmy install php-common
  - apt-get -fmy install php-curl
  - apt-get -fmy install php-dev
  - apt-get -fmy install tcl
  - apt-get -fmy install tcllib*
  - ./autogen.sh

language: c

script:
  - ./configure --enable-cpp --enable-python --enable-php --enable-tcl
  - make
  - make check
