// See http://bugs.libming.org/show_bug.cgi?id=18
//
// The bug exposed only when target SWF is > 6, so we'd need
// support for different SWF target outputs in our testsuite.
// This is currently unsupported.
//
for(;;)
{
        f = function() {};
}
